apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: archi-crab-platform
  namespace: argocd
  annotations: {argocd.argoproj.io/sync-wave: "22"}
spec:
  project: archi-crab
  source:
    repoURL: https://github.com/nausikt/CMSKubernetes.git
    targetRevision: archi-crab-testbed
    path: helm/archi-crab/platform
    helm: {valueFiles: [values.yaml]}
  destination: {server: https://kubernetes.default.svc, namespace: archi-crab}
  syncPolicy:
    # prune:false -- owns the Namespace and the Gateway (F8).
    automated: {prune: false, selfHeal: true}
    syncOptions: [CreateNamespace=true]
